# Troubleshooting

Preparation errors usually identify a missing split, image, XML, or count. Preflight aggregates missing manifests, class-count mismatches, device problems, and unavailable weights. A non-finite loss reports image IDs. Resume errors name identity or semantic config mismatches. CLI user errors return one concise line and exit code 2; use focused tests for unexpected tracebacks.
