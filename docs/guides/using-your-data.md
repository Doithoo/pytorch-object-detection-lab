# Using your data

Convert annotations to the manifest-backed dataset contract: RGB image tensors, zero-based continuous xyxy boxes, int64 labels, area, image ID, and `iscrowd`. Define stable train/valid/test splits and class ordering. Prepare and preview the complete set before training. Keep test labels out of model selection.
